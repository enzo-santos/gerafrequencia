name: example
description: A new Flutter project.
publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  intl: ^0.18.1
  provider: ^6.1.2
  fluent_ui: ^4.8.6
  file_saver: ^0.2.12
  file_picker: ^8.0.0+1
  system_theme: ^2.3.1
  window_manager: ^0.3.8
  flutter_acrylic: ^1.1.3

  gerafrequencia:
    path: ..

dev_dependencies:
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
